pipeline {
    agent any

    stages {
        stage('Webhook Test') {
            steps {
                echo 'Webhook triggered successfully'
                sh 'date'
                sh 'hostname'
            }
        }
    }
}
